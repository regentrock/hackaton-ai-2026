generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  // Adicionar connection limit para produção
  connectionLimit = 5
}

model Volunteer {
  id            String   @id @default(uuid())
  email         String   @unique
  passwordHash  String

  name          String
  location      String
  availability  String
  description   String
  skills        String[]

  createdAt     DateTime @default(now())
}